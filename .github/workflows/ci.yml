name: ci

# The public test gates: the proxy pytest suite against a disposable Postgres
# service container, the audio package's own suite (NOT collected by the
# proxy run — its testpaths live in audio/pyproject.toml), plus the dashboard
# typecheck / build / vitest, and the standalone satellite suite. Same commands
# as a local run — see CONTRIBUTING.md and proxy/tests/README.md ("CI" section).

on:
  workflow_dispatch: {}
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

# A force-push to a PR branch cancels the now-stale run; pushes to main
# always run to completion (their results guard the release flow).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  proxy:
    if: github.repository == 'OtoDock/oto-dock' || github.repository == 'frostyard/oto-dock'
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16.14-alpine
        env:
          POSTGRES_USER: otodock
          POSTGRES_PASSWORD: otodock
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U otodock"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: |
            proxy/requirements.txt
            proxy/requirements-test.txt

      - name: ruff
        # Repo-root ruff.toml — the import/dead-code hygiene gate.
        run: |
          python -m pip install -q "ruff==0.15.22"
          ruff check .

      - name: System tools used by the suite
        # The sysctl re-allows unprivileged user namespaces (Ubuntu 24.04
        # runners restrict them via AppArmor), which the sandbox
        # network-isolation integration tests need for pasta/bwrap.
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q bubblewrap passt iproute2 ripgrep
          sudo sysctl -q -w kernel.apparmor_restrict_unprivileged_userns=0

      - name: Install proxy dependencies
        # cwd = repo root: the local audio package is a path dependency.
        run: |
          python -m pip install -U pip
          python -m pip install ./audio
          python -m pip install -r proxy/requirements.txt -r proxy/requirements-test.txt

      - name: pytest
        working-directory: proxy
        env:
          TEST_DATABASE_URL: postgresql://otodock:otodock@localhost:5432/otodock_test
        run: python -m pytest -n 8 -q

      - name: pytest (audio package)
        # Colocated per-subpackage tests (providers/*/tests, streaming/tests)
        # discovered via audio/pyproject.toml testpaths.
        working-directory: audio
        run: python -m pytest -q

  dashboard:
    if: github.repository == 'OtoDock/oto-dock' || github.repository == 'frostyard/oto-dock'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: npm ci
        working-directory: dashboard
        run: npm ci --no-audit --no-fund

      - name: typecheck
        working-directory: dashboard
        run: npx tsc --noEmit

      - name: build
        working-directory: dashboard
        run: npm run build

      - name: vitest
        working-directory: dashboard
        run: npx vitest run

  satellite:
    if: github.repository == 'OtoDock/oto-dock' || github.repository == 'frostyard/oto-dock'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: |
            satellite/requirements.txt
            proxy/requirements-test.txt

      - name: Install satellite and test dependencies
        run: python -m pip install -r satellite/requirements.txt -r proxy/requirements-test.txt

      - name: pytest (satellite)
        # Run at the repository root so satellite is importable. This suite
        # is independent of the proxy's PostgreSQL fixtures and pytest.ini.
        run: python -m pytest satellite/tests -q --timeout=120

      - name: Copilot offline contract tests
        # No SDK install or inference credentials: deterministic event and
        # probe-isolation tests run independently of PostgreSQL.
        run: python -m pytest scripts/copilot/tests -q --timeout=120
