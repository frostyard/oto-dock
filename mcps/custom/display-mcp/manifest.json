{
  "name": "display-mcp",
  "server_name": "display",
  "label": "Display",
  "description": "Display images, files, audio, video, and URLs in chat",
  "version": "1.13.2",
  "category": "core",

  "server": {
    "runtime": "python",
    "transport": "stdio",
    "command": "venv/bin/python",
    "args": ["display_server.py"]
  },

  "credentials": {
    "type": "none"
  },

  "config": [],

  "env": {},
  "agent_env": {},

  "exclude_from": ["phone", "terminal", "external"],

  "tool_arg_paths": {
    "display_images": {
      "images[*].source": { "mode": "read" }
    },
    "send_file": {
      "path": { "mode": "read" }
    },
    "display_video": {
      "source": { "mode": "read" }
    },
    "display_audio": {
      "source": { "mode": "read" }
    }
  },

  "skills": [
    {
      "id": "display-tools",
      "loading": "always",
      "file": "skills/display-tools.md",
      "description": "Display MCP routing judgment: which display tool for which content, media routing rules, when display_ui is worth it, artifact iteration",
      "default_exclude_from": ["phone"]
    },
    {
      "id": "miniapp-authoring",
      "loading": "on_demand",
      "file": "skills/miniapp-authoring/SKILL.md",
      "description": "Authoring contracts for interactive artifacts, mini-app dashboards, and Dock panels: display_ui backchannel and theme events, pin_app action buttons, live data panels, feeds, scoped dashboards, file pins. Use whenever building or updating a display_ui artifact, mini-app, or dashboard.",
      "default_exclude_from": ["phone"]
    }
  ],

  "permissions": {
    "default_tier": "standard",
    "rules": [
      {
        "tool": "display_*",
        "tier": "open"
      },
      {
        "tool": "send_*",
        "tier": "open"
      },
      {
        "tool": "list_apps",
        "tier": "open"
      }
    ]
  },
  "patched": false,
  "patch_note": null
}
